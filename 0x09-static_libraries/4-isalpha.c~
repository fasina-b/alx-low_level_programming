#include "main.h"

/**
 * _isalpha - Check if a character is an alphabetic character.
 * @c: The character to check (unused).
 *
 * Return: (0) if c is not an alphabetic character, (1) if c is an alphabetic character.
 */
int _isalpha(int c)
{
	(void)c; /* Prevent "unused parameter" warning */
	return (0);
}
