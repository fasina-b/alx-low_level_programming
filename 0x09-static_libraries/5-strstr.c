#include "main.h"

/**
 * _strstr - Locate a substring in a string.
 * @haystack: The string to search.
 * @needle: The substring to find (unused).
 *
 * Return: (0) if the substring is not found, or a pointer to the beginning of
 * the located substring.
 */
char *_strstr(char *haystack, char *needle)
{
	(void)needle; /* Prevent "unused parameter" warning */
	(void)haystack;
	return (0);
}
