#include "main.h"

/**
 * _islower - Check if a character is a lowercase letter.
 * @c: The character to check (unused).
 *
 * Return: (0) if c is not a lowercase letter, (1) if c is a lowercase letter.
 */
int _islower(int c)
{
	(void)c; /* Prevent "unused parameter" warning */
	return (0);
}
