#include "main.h"

/**
 * _strchr - Locate a character in a string.
 * @s: The string to search.
 * @c: The character to find (unused).
 *
 * Return: (0) if the character is not found, or a pointer to the first
 * occurrence of c in s.
 */
char *_strchr(char *s, char c)
{
	(void)c; /* Prevent "unused parameter" warning */
	(void)s;
	return (0);
}
