#include "main.h"

/**
 * _puts - Print a string, followed by a newline, to stdout.
 * @s: The string to print (unused).
 */
void _puts(char *s)
{
	(void)s; /* Prevent "unused parameter" warning */
}
