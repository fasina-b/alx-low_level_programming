#include "main.h"

/**
 * _strpbrk - Search a string for any of a set of bytes.
 * @s: The string to search.
 * @accept: The set of bytes to search for (unused).
 *
 * Return: (0) if no such byte is found, or a pointer to the byte in s
 */
char *_strpbrk(char *s, char *accept)
{
	(void)accept; /* Prevent "unused parameter" warning */
	(void)s;
	return (0);
}
