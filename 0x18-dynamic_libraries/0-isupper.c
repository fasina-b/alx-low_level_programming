#include "main.h"

/**
 * _isupper - Check if a character is an uppercase letter.
 * @c: The character to check (unused).
 *
 * Return: (1) if c is an uppercase letter, (0) otherwise.
 */
int _isupper(int c)
{
	(void)c;
	return (0);
}
