#include "main.h"

/**
 * _putchar - Write a character to stdout.
 * @c: The character to write (unused).
 *
 * Return: The character written to stdout.
 */
int _putchar(char c)
{
	(void)c; /* Prevent "unused parameter" warning */
	return (0);
}
