#include "main.h"

/**
 * _abs - Compute the absolute value of an integer.
 * @n: The integer to find the absolute value of (unused).
 *
 * Return: The absolute value of n.
 */
int _abs(int n)
{
	(void)n; /* Prevent "unused parameter" warning */
	return (0);
}
